
# Package Tracking

This is a courier package tracking demo using react.

### `Instructions`

 - Clone the repo
 - run ``npm install``
 - Start the json-server mock API: ``npx json-server --watch db.json --port 3000`` 
 - run ``npm start``

navigate to [http://localhost:3000](http://localhost:3000) to view your application.